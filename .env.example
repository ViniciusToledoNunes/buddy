# Never commit real keys. Audio is transcribed locally and never leaves this machine.
# Only meeting text -- transcript, memory, and project excerpts -- reaches the model.
ANTHROPIC_API_KEY=

# Only for identity-linked API keys, which are rejected with HTTP 400 unless the
# request names a workspace. Find it at console.anthropic.com/settings/workspaces
# (looks like wrkspc_...). Plain workspace keys can leave this empty.
ANTHROPIC_WORKSPACE_ID=

# Optional: point Buddy at a local Ollama server instead of Claude.
# OLLAMA_BASE_URL=http://127.0.0.1:11434

# Optional: allow the MCP server to start a recording. Off by default.
BUDDY_ALLOW_MCP_START=false

# Optional: read-only Jira for the investigator. It registers its tools only when all
# three exist. Token: id.atlassian.com/manage-profile/security/api-tokens
JIRA_URL=
JIRA_EMAIL=
JIRA_API_TOKEN=

# BigQuery needs no variables here: Buddy runs as the same user as gcloud and
# inherits `gcloud auth` credentials.

# Optional: read-only Datadog for the investigator. All three are required -- without
# DD_SITE the base URL cannot be built.
DD_SITE=
DD_API_KEY=
DD_APP_KEY=
